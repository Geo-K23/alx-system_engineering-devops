# Finding out why Apache is returning a 500 error, fixing and automating it using Puppet

exec { 'fix-wordpress':
  command => 'sed -i s/phpp/php/g /var/www/html/wp-settings.php',
  path    => '/usr/local/bin/:/bin/'
}
